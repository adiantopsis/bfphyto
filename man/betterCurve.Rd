% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betterCurve.R
\name{betterCurve}
\alias{betterCurve}
\title{Generate species accumulation curves for phytosociological data}
\usage{
betterCurve(
  x,
  plot_area,
  method = "collector",
  value = "cob",
  colour = "lightgreen",
  q = 0,
  lang = "pt"
)
}
\arguments{
\item{x}{Data frame containing phytosociological data in long format, with mandatory columns \code{parc} (plot), \code{spp} (species), and the variable specified in \code{value}.}

\item{plot_area}{Numeric. Area (m²) of each plot or sampling unit, used to calculate accumulated area.}

\item{method}{Character. Method for calculating the accumulation curve. Must be one of: \code{"collector"}, \code{"exact"}, \code{"random"}, or \code{"inext"}.}

\item{value}{Character. Variable used for calculation: \code{"cob"} (coverage) or another variable indicating occurrence counts.}

\item{colour}{Character. Color used in the plot for the ribbon and points of the curve (e.g., \code{"lightgreen"}).}

\item{q}{Numeric. Order of Hill number for the \code{"inext"} method. Default is \code{0}, corresponding to species richness.}

\item{lang}{Character. Language for axis labels and legends. Can be \code{"pt"} (Portuguese) or \code{"us"} (English). Default is \code{"pt"}.}
}
\value{
A list with two elements:
\describe{
\item{plot}{A \code{ggplot} object with the species accumulation curve plot.}
\item{result}{An object with the calculation results of the selected method (\code{iNEXT} or \code{specaccum}).}
}
}
\description{
This function computes and plots species accumulation curves based on phytosociological data,
using different accumulation methods: \code{"collector"}, \code{"exact"}, \code{"random"}, and \code{"inext"}.
It relies on the packages \pkg{vegan}, \pkg{iNEXT}, \pkg{reshape2}, \pkg{dplyr}, and \pkg{ggplot2}.
}
\details{
For the \code{"inext"} method, the function performs rarefaction and extrapolation estimates using \code{iNEXT::iNEXT} and plots them with \code{iNEXT::ggiNEXT}.
For the \code{"collector"}, \code{"exact"}, and \code{"random"} methods, it uses \code{vegan::specaccum} to compute accumulation curves and plots with \pkg{ggplot2}.

The function transforms data into a presence/absence matrix before calculations.
}
\examples{
\dontrun{
# Assuming 'data' is a data.frame with columns 'parc', 'spp', and 'cob'
betterCurve(data, plot_area = 100, method = "collector", value = "cob", colour = "blue", lang = "us")

betterCurve(data, plot_area = 100, method = "inext", q = 0, lang = "us")
}

}
